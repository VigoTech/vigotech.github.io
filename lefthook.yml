pre-commit:
  commands:
    lint:
      run: pnpm lint
    format:
      run: pnpm format:check
    type-check:
      run: pnpm type-check
    test:
      run: pnpm test:coverage

commit-msg:
  commands:
    commitlint:
      run: pnpm commitlint --edit {1}
